// copy the array
const arr = [1, 2, 3, 4, 5,];
console.log(arr);
const arr1 = arr.slice(0);
console.log(arr1);

for (let i = 0; i < arr.length; i += 1) {
    // console.log(arr[i]);
    var arr3 = [];
    arr3 = arr[i]
    // arr3.push(arr[i])
    console.log(arr3);
}